# The `std.collections` package: current Mojo's import spellings for the
# collection types (`from std.collections import Set`); the prelude exports
# the same names.

from .dict import Dict
from .list import List
from .set import Set
from std.optional import Optional, OptionalReg
