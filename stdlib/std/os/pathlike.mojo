"""The `PathLike` trait: a value that names a filesystem path."""

# Declared beside `String` (which conforms to it) in `std.string`; this is
# upstream's import path.
from std.string import PathLike
