"""Path predicates and string manipulation (`os.path`)."""

from .path import (
    basename,
    dirname,
    exists,
    expanduser,
    expandvars,
    getsize,
    is_absolute,
    isdir,
    isfile,
    islink,
    join,
    lexists,
    split,
    split_extension,
    splitroot,
)
