//! Callable identity: the `$ov$` overload symbol scheme, specialization
//! mangling, and symbol resolution helpers. The single owner of lowered
//! symbol spelling (see `docs/symbol-map.md`).

pub mod symbol;
