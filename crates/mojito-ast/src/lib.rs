//! Surface syntax: the AST node vocabulary and the structural call-binding
//! policy (`call`) that owns argument-to-parameter matching. Depends only on
//! `mojito-common`.

pub mod ast;
pub mod call;
