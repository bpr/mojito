[package]
name = "pliron-stage0-spike"
version = "0.1.0"
edition = "2024"
rust-version = "1.96.1"
publish = false

[dependencies]
pliron = { git = "https://github.com/pliron-org/pliron", rev = "477e6b0edb18b29df4cf7b90f0f468dc8a872f22" }
pliron-llvm = { git = "https://github.com/pliron-org/pliron", rev = "477e6b0edb18b29df4cf7b90f0f468dc8a872f22" }

[dev-dependencies]
expect-test = "1.5.1"
tempfile = "3.20.0"

# Deliberately not a member of any workspace: the production `mojito` crate and
# its `scripts/check` gate must never build or link LLVM. See
# docs/notes/pliron-stage0.md for the pin record and discovery configuration.
[workspace]
